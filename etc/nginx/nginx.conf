events {}

http {
    include /etc/nginx/mime.types;
    include /etc/nginx/uwsgi_params;

    access_log /var/log/stupidchess-nginx.log;
    error_log /var/log/stupidchess-nginx-errors.log;

    server {
        listen 23080;

        # Android favicons
        location = /manifest.json {
            root /opt/stupidchess/dist/favicons;
        }

        location ~ ^/android-chrome-.*.png$ {
            root /opt/stupidchess/dist/favicons;
        }


        # Apple favicons
        location = /apple-touch-icon.png {
            root /opt/stupidchess/dist/favicons;
        }

        location = /safari-pinned-tab.svg {
            root /opt/stupidchess/dist/favicons;
        }


        # Microsoft favicons
        location ~ ^/mstile.* {
            root /opt/stupidchess/dist/favicons;
        }

        location = /browserconfig.xml {
            root /opt/stupidchess/dist/favicons;
        }


        # Web favicons
        location ~ ^/favicon.* {
            root /opt/stupidchess/dist/favicons;
        }


        # Application files
        location = /_version.json {
            root /opt/stupidchess/dist/;
        }

        location ~ ^/(js|css|img)/ {
            root /opt/stupidchess/dist/;
        }

        location / {
            uwsgi_pass unix:///var/run/stupidchess.sock;
        }
    }
}
